{ "exit_code": 1, "reason": "statement_mismatch", "detail_contains": "Challenge.M0.absurd",
  "note": "DESIGN section 11 predicted not_proved. `Include M0` does not re-declare absurd: it makes Challenge.absurd a delta alias whose CANONICAL name is Challenge.M0.absurd, i.e. a different kernel object from the one the challenge named. The canonical-name check now rejects that on the target itself, one step before the axiom check that used to catch the admitted proof one level down (previous expectation: forbidden_axiom / Challenge.M0.absurd). Same exit code, same culprit named, and the reason is now about the real defect: the solution never defined a constant called Challenge.absurd." }
