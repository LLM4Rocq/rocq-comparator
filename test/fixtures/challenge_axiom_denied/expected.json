{ "exit_code": 0,
  "note": "Same challenge, but the solution PROVES the admitted helper instead of re-admitting it (Undef becomes OpaqueDef). Discharging one of the challenge's own assumptions is strictly stronger than leaning on it, so it is accepted: the pin on a challenge axiom demands the same statement and allows Undef, Def or OpaqueDef -- unlike a permitted_axioms entry from the config, which must still be Undef. Observed verdict: ok, targets[0].assumptions = [\"Challenge.P\", \"Challenge.P_true\"], i.e. helper is gone from the assumptions because it is no longer one." }
