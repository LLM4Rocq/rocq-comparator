{ "exit_code": 1, "reason": "library_violation", "prebuild": ["Stdlib/Arith.v"],
  "note": "The Collatz cheat from talks/2026_caia/collatz: a fake Stdlib/Arith.v (nat with a single constructor) is bound to the logical namespace Stdlib by -Q . \"\", shadowing the real standard library for BOTH the challenge and the solution, so every content check passes consistently and only the load-path shadowing check rejects it. Detection is list-free: the protected namespaces are read from the switch's own load path at run time. The runner compiles the files in \"prebuild\" with rocq -Q <copy> \"\" in a scratch copy before running the comparator, so no .vo lands in the repo." }
